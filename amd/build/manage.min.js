define("enrol_apply/manage",["exports"],(function(_exports){Object.defineProperty(_exports,"__esModule",{value:!0}),_exports.init=void 0;
/**
   * Select-all handling for the enrolment application queue.
   *
   * The form submits through its own button, so this module is a progressive enhancement
   * only: with JavaScript disabled every checkbox is still operable one by one.
   *
   * @module     enrol_apply/manage
   * @copyright  2026 Anderson Blaine
   * @copyright  2016 sudile GbR (http://www.sudile.com)
   * @license    http://www.gnu.org/copyleft/gpl.html GNU GPL v3 or later
   */
const SELECTORS_FORM="#enrol_apply_manage_form",SELECTORS_TOGGLE_ALL='[data-action="toggleall"]',SELECTORS_ROW_CHECKBOX='input[name="userenrolments[]"]';_exports.init=()=>{const form=document.querySelector(SELECTORS_FORM);if(!form)return;const toggleall=form.querySelector(SELECTORS_TOGGLE_ALL);if(!toggleall)return;const rows=()=>Array.from(form.querySelectorAll(SELECTORS_ROW_CHECKBOX));toggleall.addEventListener("change",(()=>{rows().forEach((checkbox=>{checkbox.checked=toggleall.checked}))})),form.addEventListener("change",(event=>{if(!event.target.matches(SELECTORS_ROW_CHECKBOX))return;const all=rows(),checked=all.filter((checkbox=>checkbox.checked));toggleall.checked=all.length>0&&checked.length===all.length,toggleall.indeterminate=checked.length>0&&checked.length<all.length}))}}));

//# sourceMappingURL=manage.min.js.map