# AS: offer the way back to the queue even to an operator who may open none, which is an
# invitation to the refusal that scope()'s fourth branch exists to avoid.
s#'hasqueueurl' => \$this->queueurl !== null,#'hasqueueurl' => true,#s;
