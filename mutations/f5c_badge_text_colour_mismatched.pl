# F5C: put white text on the light waiting-list badge (1.95:1). A text utility is present, so a
# check accepting any of them passes it; only the pairing catches it.
s{'badge bg-warning text-dark me-1'}{'badge bg-warning text-white me-1'}s;
